<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/updateprofile" method="post">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username" value="<%= user.Username %>" required>

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="<%= user.email %>" required>

        <label for='address'>Address:</label>
        <input type="address" id="address" name="Address" value="<%= profiledetails.length > 0 &&profiledetails[0].datas.length > 0 ? profiledetails[0].datas[0].Address : '' %>" required>

        <label for='age'>Age:</label>
        <input type="age" id="age" name="age" value="<%= profiledetails.length > 0 && profiledetails[0].datas.length > 0 ? profiledetails[0].datas[0].age : '' %>" required>

        <label for='phone'>PhoneNumber:</label>
        <input type="phone" id="phone" name="phoneNumber" value="<%= profiledetails.length > 0 && profiledetails[0].datas.length > 0 ? profiledetails[0].datas[0].phoneNumber : '' %>" required>

        <button type="submit">Update Profile</button>

    </form>

</body>
</html>